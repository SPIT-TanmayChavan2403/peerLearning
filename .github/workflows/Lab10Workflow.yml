name: Workflow to deploy project on firebase

on:
  push:
    branches:
      - master

jobs:
  firebase-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
        with:
          node-version: "16.x"
      - run: npm install
      - run: npm run build
      - name: Deploy to firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: 1//0gyPiB2rEuaCJCgYIARAAGBASNgF-L9IrWVb7zih9lqkOXOlxTlLuC2Vj2zF8M6LokBZ5W12SuKBKtdtAlSJXGSN2izVvZxwmSA
